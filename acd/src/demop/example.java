package demop;

public class example {
	
	
	public static void main(String[] args) 
	{
		String a = "100%";
		String value = a.substring(0, a.length()-1);
		double value1 = Double.parseDouble(value);
		
		System.out.println(value1);
	
//	float valuel float.parsefloat (approval rate target hvaw.substring(0, approval rate target hysw.length()-1));
//	float value2 float.parsefloat(approval rate control hvow.substring(0, approval rate control hvsw.length()-1));
//	5min 20 sec float expected us value1/value2;
//	float actual es float.parsefloat(effect size hvaw); 
//	system.out.println(actual es); 
//	assert.assertequals(expected es, actual_es,delta);

	}
}
